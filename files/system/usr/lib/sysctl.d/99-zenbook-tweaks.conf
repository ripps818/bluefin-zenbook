# Enable BBR for better network throughput/latency (CachyOS default)
net.core.default_qdisc = fq
net.ipv4.tcp_congestion_control = bbr

# Prefer swapping to ZRAM (fast) over dropping file caches (slow to reload)
vm.swappiness = 100
